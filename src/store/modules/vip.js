// vip相关数据

const state = {
    selected_vip: null
};

const mutations = {
    set_selected_vip(state, vip) {
        state.selected_vip = vip;
    }
};

const getters = {
};

const actions = {
};

export default {
    state,
    getters,
    mutations,
    actions
};
